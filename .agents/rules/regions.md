# Region × Source

- 市場は `JP`、開示 Source は `EDINET`。Region と Source を同一概念にしない。
- 探索スクリプトは `scripts/jp/edinet/`、探索 cache は `tmp_cache/edinet/` に置く。
- 共有レイヤに Source 固有のファイル名規則を埋め込まない。
- EU / ESEF 製品トラックは中止（Linear BLT-15/16/17 Canceled）。再配線しない。
