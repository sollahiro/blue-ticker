# コミットメッセージ

Conventional Commits。英語・小文字始まり・命令形・末尾ピリオドなし。スコープ（`feat(auth):`）は使わない。

`feat` / `fix` / `refactor` / `docs` / `chore` / `test` / `perf`

1コミット＝1論理変更。バージョンバンプは機能コミットと分け、独立した `chore:` にする。
