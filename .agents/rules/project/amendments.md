# 訂正有報 (130)

自動マージは作らない。数値差し替え・タグ欠損・科目削除が混在する。

sync は 130 を別行で取り込む（`parentDocID` なし。150/170 は対象外）。ingest の正本は有報 (120)。財務 high-water だけ 130 を含むが、読む ZIP は原本のまま。

専用キューは作らない。`edinet_documents` を `doc_type_code = '130'` で手動確認する（まず日経225と格納済みの期）。同じ期末の原本と抽出差分を人間が判断する。

**見た 130 の docID は、原本準拠でも Git に残す。** 残さないと同じ 130 が再掲される。訂正を正本にしたときだけ、期末の正本 docID も残す（残さないと次の ingest が 120 に戻す）。
